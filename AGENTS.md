# 開発ガイド

このファイルは、TEN. リポジトリで作業するエージェント向けの短いルールです。実装の詳細や変更されやすいファイル一覧はここに複製せず、コードと各文書を参照してください。

## 基本方針

- 依頼の範囲を確認し、必要な変更だけを行う
- 既存の変更や未コミットの変更を勝手に戻さない
- プロジェクトの既存の書式、命名、依存関係を優先する
- 仕様と実装が異なる場合は、実装を確認してから文書を更新する
- 秘密情報、APIトークン、個人情報をファイルやログへ記録しない
- 互換性対応は、保存データ・外部利用者・公開済みの動作など具体的な理由がある場合だけ追加する

## 作業前

- リポジトリのルートと作業対象を確認する
- 関係するコード、テスト、文書、設定を読んでから編集する
- 依存関係や公開設定を変更する場合は、変更の影響範囲を確認する
- デプロイ設定や秘密情報を変更する場合は、対応する `docs/deployment` も確認する

## 実装ルール

- 共有される処理は、利用箇所ごとの重複実装ではなく共有パッケージを検討する
- 入力値は境界で検証し、認証・認可をクライアントの判定だけに依存させない
- 永続データの形式を変更するときは、既存データの移行と失敗時の動作を考える
- 適用済みのデータベースマイグレーションを書き換えず、後続の変更を新しいマイグレーションで追加する
- ユーザー向けの文字列を追加・変更するときは、対応する翻訳と表示幅を確認する
- UIを変更するときは、キーボード操作、タッチ操作、狭い画面、Reduced Motionを確認する
- 新しい依存関係は、既存の依存関係で代替できないか確認してから追加する

## 確認

変更範囲に応じて、少なくとも次を実行します。

```bash
pnpm format:check
pnpm lint
pnpm typecheck
pnpm test
```

通常のコード変更では、最後に次も実行します。

```bash
pnpm check
pnpm build
```

ゲームルール、保存形式、API、デプロイ設定を変更した場合は、関連するテストと文書も更新します。テストやビルドを実行できない場合は、その理由を報告します。

## 文書ルール

- `README.md` はリポジトリの入口と最小限の開発案内にする
- `docs/product` はプロダクトの目的・仕様・実装状況を扱う
- `docs/deployment` は利用者が公開・配布・運用するための手順を扱う（構成図・設定表・通常フロー・初回手順・トラブルシューティングの統一構成とし、非技術者でも迷わず操作できる簡潔で十分な記述にする）
- 同じ手順を複数の文書へ書かず、詳細を持つ文書へリンクする
- 実装済み、試作表示、将来案を明確に区別する
- 実在するコマンド、ファイル、環境変数だけを案内する
- コマンドは可能な限りリポジトリのルートから実行できる形にする
- 手順には前提条件、実行結果の確認方法、失敗時の確認先を含める
